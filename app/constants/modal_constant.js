import {getStyleOfDevice} from '../utils/responsive_util';

export const provincePickerSnapPoints = getStyleOfDevice(['60%'], ['65%']);
export const provincePickerContentHeight = getStyleOfDevice('58%', '59%')