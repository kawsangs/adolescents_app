import React from 'react';
import {View} from 'react-native';
import {Card, Title} from 'react-native-paper';



const VideoItemListComponent = (props) => {
  const renderCards = () => {
    const videos = [
      {id: 1,  name: "អារម្មណ៏នៅពេលពេញវ័យ", url: ""}
    ]
  }

  return (

  )
}

export default VideoItemListComponent;