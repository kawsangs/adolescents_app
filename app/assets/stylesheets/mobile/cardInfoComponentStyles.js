import { StyleSheet } from 'react-native';

const cardInfoComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    paddingBottom: 8,
    paddingTop: 10
  },
  arrowIcon: {
    alignSelf: 'flex-end',
    height: 30
  }
});

export default cardInfoComponentStyles;